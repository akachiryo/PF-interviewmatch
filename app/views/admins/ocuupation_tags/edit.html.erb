<h2>職種編集</h2>
<%= form_with model: @ocuupation_tag, url: admins_ocuupation_tag_path(@ocuupation_tag), local: true do |f| %>
  <%= f.label:name, "職種名" %>
  <%= f.text_field :name %>
  <%= f.submit "変更を保存" %>
<% end %>
